import SearchBar from "./SearchBar";

function Header() {

    return (
        <header>
            <img src="/boolflix.png" alt="logo" />
            <SearchBar />
        </header>
    );
}

export default Header;
